<h1> {{ title }} </h1>
<p> Welcome to {{ title }} </p>
{{# if condition }}
    condition met
    {{else}}
    condition not met
    {{/if}}
<br>
<hr>
{{# each anyArray as |val key|}}

    {{key}}:{{val}}
{{/each}}